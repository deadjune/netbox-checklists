from setuptools import find_packages, setup

setup(
    name='netbox-checklists',
    version='0.1',
    description='Netbox plugin for creating checklists',
    url='',
    author='Anna Krymina',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)